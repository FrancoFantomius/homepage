What to do when editing this file:
- Do not run ```npm run build```, there are no errors it can trow.
- When adding an asset, add it also to the sw.js CORE_ASSETS to cache it.
- Prefer local assets instead of CDNs.
- Check the "./node_modules/@francofantomius/material-components/llms.txt" docs for the material components implementation.